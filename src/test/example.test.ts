import { describe, it, expect } from "vitest";

describe("example", () => {
  it("should pass", () => {
    // Учебная тревога: намеренно сломанный тест для проверки Telegram-уведомлений о сбоях CI
    expect(true).toBe(false);
  });
});
